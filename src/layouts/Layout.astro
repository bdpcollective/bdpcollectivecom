---
import Footer from '../components/Footer.astro';
import { baseUrl } from '../config';

interface Props {
	title: string;
}
---

<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href={`${baseUrl}/favicon.svg`} />
		<meta name="generator" content={Astro.generator} />
		<title>Brian Pope | bdpcollective</title>

		<!-- DNS prefetch for external domains -->
		<link rel="dns-prefetch" href="https://fonts.googleapis.com">
		<link rel="dns-prefetch" href="https://fonts.gstatic.com">
		<link rel="dns-prefetch" href="https://www.googletagmanager.com">

		<!-- Preconnect to critical domains ASAP -->
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link rel="preconnect" href="https://www.googletagmanager.com">

		<!-- Preload critical font file for faster rendering -->
		<link rel="preload" as="font" type="font/woff2" href="https://fonts.gstatic.com/s/librefranklin/v14/jizDREVItHgc8qDIbSTKq4XkRg8T88bjFuXOnduhLsSUB9rIb-JH1g.woff2" crossorigin>

		<!-- Async Google Fonts with font-display: swap for non-blocking load -->
		<link href="https://fonts.googleapis.com/css2?family=Libre+Franklin:wght@400;600;700&display=swap" rel="stylesheet" media="print" onload="this.media='all'; this.onload=null;">
		<noscript>
			<link href="https://fonts.googleapis.com/css2?family=Libre+Franklin:wght@400;600;700&display=swap" rel="stylesheet">
		</noscript>

		<!-- Inline Critical CSS for faster initial render -->
		<style>
			:root {
				--accent: 136, 58, 234;
				--accent-light: 224, 204, 250;
				--accent-dark: 49, 10, 101;
				--accent-gradient: linear-gradient(45deg, rgb(var(--accent)), rgb(var(--accent-light)) 30%, white 60%);
			}
			html {
				font-family: "Libre Franklin", "Franklin Gothic", "ITC Franklin Gothic", "Franklin Gothic Medium", "Franklin Gothic Book", "Franklin Gothic Demi", "Franklin Gothic Heavy", system-ui, sans-serif;
				background: #f6f6f6;
				background-size: 224px;
			}
			body {
				margin: 0;
				padding: 0;
				box-sizing: border-box;
				overflow-x: hidden;
				overflow-y: auto !important;
			}
			html, body {
				overflow: auto !important;
				height: auto !important;
			}
			/* Prevent layout shift for images */
			img {
				max-width: 100%;
				height: auto;
			}
			/* Critical above-fold styles for mobile */
			@media (max-width: 768px) {
				h1 {
					font-size: 2.25rem;
					line-height: 2.5rem;
				}
				.container {
					padding-left: 1rem;
					padding-right: 1rem;
				}
			}
		</style>

		<!-- Google Analytics - Loaded with requestIdleCallback for better performance -->
		<script is:inline>
			// Load Google Analytics during browser idle time to avoid blocking main thread
			(function() {
				var loadGA = function() {
					var script = document.createElement('script');
					script.async = true;
					script.src = 'https://www.googletagmanager.com/gtag/js?id=G-8B6D7GXRG2';
					document.head.appendChild(script);

					window.dataLayer = window.dataLayer || [];
					function gtag(){dataLayer.push(arguments);}
					gtag('js', new Date());
					gtag('config', 'G-8B6D7GXRG2');
				};

				// Use requestIdleCallback if available, otherwise fallback to setTimeout
				if ('requestIdleCallback' in window) {
					requestIdleCallback(loadGA, { timeout: 2000 });
				} else {
					setTimeout(loadGA, 2000);
				}
			})();
		</script>

		<slot name="head" />
	</head>
	<body class="min-h-screen flex flex-col font-franklin">
		<nav class="bg-white shadow-sm">
			<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
				<div class="flex justify-between h-16">
					<div class="flex">
						<a href={`${baseUrl}/`} class="flex items-center">
							<span class="text-xl font-bold">BRIAN POPE</span>
						</a>
					</div>
					<div class="hidden md:flex items-center space-x-4">
						<a href={`${baseUrl}/`} class="text-gray-700 hover:text-gray-900">Home</a>
						<a href={`${baseUrl}/ux-portfolio`} class="text-gray-700 hover:text-gray-900">UX Portfolio</a>
						<a href={`${baseUrl}/logos`} class="text-gray-700 hover:text-gray-900">Logos</a>
						<a href={`${baseUrl}/data-visualization`} class="text-gray-700 hover:text-gray-900">Data Visualization</a>
						<a href={`${baseUrl}/images`} class="text-gray-700 hover:text-gray-900">Images</a>
					</div>
					<div class="md:hidden flex items-center">
						<button id="mobile-menu-button" class="text-gray-700 hover:text-gray-900 focus:outline-none" aria-label="Open menu">
							<svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
								<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
							</svg>
						</button>
					</div>
				</div>
			</div>
			<!-- Mobile menu -->
			<div id="mobile-menu" class="hidden md:hidden fixed inset-0 z-50 bg-white overflow-y-auto">
				<div class="flex justify-end p-4">
					<button id="close-menu-button" class="text-gray-700 hover:text-gray-900 focus:outline-none" aria-label="Close menu">
						<svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
							<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
						</svg>
					</button>
				</div>
				<div class="flex flex-col items-center space-y-6 py-8">
					<a href={`${baseUrl}/`} class="text-xl text-gray-700 hover:text-gray-900">Home</a>
					<a href={`${baseUrl}/ux-portfolio`} class="text-xl text-gray-700 hover:text-gray-900">UX Portfolio</a>
					<a href={`${baseUrl}/logos`} class="text-xl text-gray-700 hover:text-gray-900">Logos</a>
					<a href={`${baseUrl}/data-visualization`} class="text-xl text-gray-700 hover:text-gray-900">Data Visualization</a>
					<a href={`${baseUrl}/images`} class="text-xl text-gray-700 hover:text-gray-900">Images</a>
				</div>
			</div>
		</nav>

		<main class="flex-grow">
			<slot />
		</main>

		<Footer />
	</body>
</html>

<style is:global>
	/* Non-critical styles loaded after initial render */
	code {
		font-family: Menlo, Monaco, Lucida Console, Liberation Mono, DejaVu Sans Mono,
			Bitstream Vera Sans Mono, Courier New, monospace;
	}
</style>

<script>
	// Mobile menu functionality - optimized with event delegation
	const menu = document.getElementById('mobile-menu');

	if (menu) {
		const toggle = (show: boolean) => {
			menu.classList.toggle('hidden', !show);
			document.body.style.overflow = show ? 'hidden' : '';
		};

		// Use event delegation for better performance (single listener instead of multiple)
		document.addEventListener('click', (e: MouseEvent) => {
			const target = e.target as HTMLElement;
			if (target.closest('#mobile-menu-button')) {
				toggle(true);
			} else if (target.closest('#close-menu-button') || target === menu || target.closest('#mobile-menu a')) {
				toggle(false);
			}
		});

		// Handle escape key to close menu
		document.addEventListener('keydown', (e: KeyboardEvent) => {
			if (e.key === 'Escape' && !menu.classList.contains('hidden')) {
				toggle(false);
			}
		});
	}
</script>
